const hourValue = Number(prompt('Введите количество часов', 5));

const secondsValue = (hourValue * 60) * 60;
alert(`Это будет ${secondsValue} секунд`);

// if(Number(hourValue)) {

//     const secondsValue = (hourValue * 60) * 60;
//     alert(`Это будет ${secondsValue} секунд`);

// } else {

//     alert('С числом выйдет лучше) Введите число!!!')

// }
